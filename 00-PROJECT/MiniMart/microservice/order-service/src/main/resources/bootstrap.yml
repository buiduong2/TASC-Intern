spring:
    application:
        name: order-service

    output:
        ansi:
            enabled: always

    profiles:
        active:
            - ${PROFILE:dev}
    cloud:
        config:
            uri: ${CONFIG_SERVER_URI:http://localhost:8071}
