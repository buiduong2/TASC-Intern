spring:
    application:
        name: gateway-server

    output:
        ansi:
            enabled: always

    profiles:
        active:
            - ${SPRING_PROFILES_ACTIVE:dev}

    cloud:
        config:
            uri: ${CONFIG_SERVER_URI:http://localhost:8071}
