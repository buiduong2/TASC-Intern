spring:
    application:
        name: eureka-server

    output:
        ansi:
            enabled: always

    profiles:
        active:
            - ${SPRING_PROFILES_ACTIVE:dev}
    config:
        import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8071}
